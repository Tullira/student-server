import { Router } from 'express';
import { {{pascalCase name}}Controller } from '@controllers';

const {{pascalCase name}}Router = Router();

{{pascalCase name}}Router.route('/').post({{pascalCase name}}Controller.create);
{{pascalCase name}}Router.route('/').get({{pascalCase name}}Controller.readAll);
{{pascalCase name}}Router.route('/:{{camelCase name}}Id').get({{pascalCase name}}Controller.read);
{{pascalCase name}}Router.route('/:{{camelCase name}}Id').patch({{pascalCase name}}Controller.update);
{{pascalCase name}}Router.route('/:{{camelCase name}}Id').delete({{pascalCase name}}Controller.delete);

export default {{pascalCase name}}Router;
